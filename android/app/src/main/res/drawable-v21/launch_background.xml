<?xml version="1.0" encoding="utf-8"?>
<!-- Same as drawable/launch_background.xml. The v21 copy exists because Flutter's
     template points the launch theme here on API 21+, and the stock version deferred
     to ?android:colorBackground — which is white, or black in dark mode. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/launch_ground" />
</layer-list>
