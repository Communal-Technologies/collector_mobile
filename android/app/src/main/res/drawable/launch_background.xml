<?xml version="1.0" encoding="utf-8"?>
<!-- The window Android paints before Flutter's first frame. It is the brand purple
     so the cold start runs straight into the splash screen instead of flashing white
     on the way. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/brand_purple" />
</layer-list>
